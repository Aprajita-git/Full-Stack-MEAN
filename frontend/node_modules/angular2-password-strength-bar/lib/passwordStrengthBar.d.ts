import { OnChanges, EventEmitter, SimpleChange } from '@angular/core';
export declare class PasswordStrengthBar implements OnChanges {
    passwordToCheck: string;
    barLabel: string;
    strengthChanged: EventEmitter<{}>;
    bar0: string;
    bar1: string;
    bar2: string;
    bar3: string;
    bar4: string;
    private colors;
    private measureStrength(p);
    private getColor(s);
    getStrengthIndexAndColor(strength: any): {
        idx: number;
        col: string;
    };
    ngOnChanges(changes: {
        [propName: string]: SimpleChange;
    }): void;
    private setBarColors(count, col);
}
